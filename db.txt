https://www.digitalocean.com/community/tutorials/how-to-use-postgresql-with-node-js-on-ubuntu-20-04


$ sudo -u postgres psql

postgres=# CREATE USER <dbuser> WITH PASSWORD <dbpassword>;
CREATE ROLE

postgres=# CREATE DATABASE <dbname>;
CREATE DATABASE

postgres=# GRANT ALL PRIVILEGES ON DATABASE <dbname> TO <dbuser>;
GRANT

postgres=# \du
postgres=# \dt
postgres=# \l

# \l

CREATE TABLE users(
    id SERIAL PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(50) UNIQUE NOT NULL,
    password VARCHAR(128) NOT NULL,
    role VARCHAR(20) NOT NULL
);
INSERT INTO users(username, email, password, role) values('nkhaskho', 'nkh@email.com', 'pass1234', 'admin');

CREATE TABLE categories(
    id SERIAL PRIMARY KEY,
    name VARCHAR(50) UNIQUE NOT NULL
);
INSERT INTO categories(name) values('Administration');

postgres=# \list
\connect dbname 

CREATE TABLE tickets(
    id SERIAL PRIMARY KEY,
    title VARCHAR(128) NOT NULL,
    description VARCHAR(255) NOT NULL,
    createdAt TIMESTAMP DEFAULT NOW(),
    closedAt TIMESTAMP,
    status VARCHAR(30) NOT NULL
);  